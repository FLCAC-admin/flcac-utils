[pytest]
# Only collect test_*.py so ad-hoc scripts like flow_unit_test.py are not
# imported as test modules (their top-level code would run and hit the network).
python_files = test_*.py
markers =
    network: calls the FLCAC HTTP API (use -m "not network" to skip)
